version: 2

updates:
- package-ecosystem: github-actions
  directory: /
  schedule:
    interval: daily
  cooldown:
    default-days: 30
  allow:
  - update-types: ["version-update:semver-major"]
